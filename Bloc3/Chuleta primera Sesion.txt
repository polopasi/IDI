

Hay que pasar TODO en SCO.

SCA --> SCO    View*
SCO --> SCO    nada
SCM --> SCO    View*TG*          //Si los focos de la luz se movieran con el modelo (ejemplo: faros coches)


Calcular la normalSCO:

normal --> normalSCO:  NM = insversa de la traspuesta de View*TG*

normalSCO = NM*Normal

Calcular el vector L:

L = posFocusSCO - posVerticeSCO
                    ^^^
                  View*TG*vertex
L = normalize (L)



//Del vertexShader
    //mat3 NormalMatrix = inverse (transpose (mat3 (View * TG)));

    //vec4 vertSCO = view*TG*vec4 (vertex, 1.0);
    //vec4 posFocusSCO = View*vec4(posFocus, 1);
    //vec4 L = posFocusSCO.xyz
    //La solucion es fcolor = Ambiente() + Difus(...) + Especular (...)
    vec3 normSCO = NormalMatrix*normal;
    fcolor = matdiff + Ambient() + Difus (normSCO);
    gl_Position = proj * view * TG * vec4 (vertex, 1.0);
